# Skill Writer Prompt — Post-Implementation Learning Capture

You have just completed a successful implementation run on this project.
Your job now is to **create or update `skill.md`** in the project root with
everything you learned, so that future runs on this project are faster and
make fewer mistakes.

## External Project

The Spring Boot project is located at: `{{project_path}}`

## Completed task.md

```
{{task_md}}
```

## Existing skill.md

{{existing_skill_md}}

## Instructions

1. **Explore the project** to capture its real structure and conventions:
   - `list_files(".")`, `list_files("src/main/java")`, `list_files("src/test/java")`
   - `read_file("pom.xml")` for dependencies, Java version, base package
   - Read 2-3 representative source files to capture coding patterns

2. **Extract learnings from this run** using the task.md above:
   - Which tasks required rework? What was the root cause?
   - Which patterns did the reviewer flag?
   - What test failures occurred and why?

3. **If `skill.md` already exists**, merge new learnings into it — do NOT
   discard previous knowledge. Add to each section and increment the
   `runs_completed` counter.

4. **Write `skill.md`** using `write_file("skill.md", ...)` with this structure:

```markdown
# Project Skill File
> Auto-generated by the multi-agent platform. Helps agents work faster on repeat runs.
> **runs_completed**: <N>
> **last_updated**: <date of this run>

## Project Overview
- **framework**: Spring Boot <version>
- **java_version**: <version>
- **build_tool**: Maven
- **base_package**: <e.g., com.example.demo>
- **group_id**: <from pom.xml>
- **artifact_id**: <from pom.xml>

## Package Structure
<Map of the actual package tree, e.g.:>
- `com.example.demo.controller` — REST controllers
- `com.example.demo.service` — Business logic interfaces + impls
- `com.example.demo.model` — JPA entities
- `com.example.demo.dto` — Request/response DTOs
- `com.example.demo.repository` — Spring Data repositories
- `com.example.demo.exception` — Custom exceptions + handler

## Key Dependencies
<List the important dependencies from pom.xml, e.g.:>
- spring-boot-starter-web
- spring-boot-starter-data-jpa
- spring-boot-starter-validation
- lombok (if present)
- h2 / postgresql (database)
- springdoc-openapi (if present)

## Architecture Patterns
<Document the actual patterns found in the codebase:>
- Controllers: @RestController, constructor injection, ResponseEntity returns
- Services: Interface + Impl pattern (or direct @Service classes)
- DTOs: Java records vs. classes vs. Lombok @Data
- Validation: jakarta.validation annotations on DTOs
- Exception handling: @ControllerAdvice / GlobalExceptionHandler pattern
- Entity mapping: Manual vs. MapStruct vs. ModelMapper

## Testing Patterns
- Test framework: JUnit 5 + Spring Boot Test
- Mocking: @MockBean / Mockito.mock()
- Controller tests: @WebMvcTest + MockMvc
- Service tests: Plain unit tests with mocked repos
- Assertion style: assertEquals / assertThat (Hamcrest vs. AssertJ)
- Test data: Builders, static factory methods, or inline construction

## Build & Test Commands
- Compile: `mvn compile -q`
- Test: `mvn test`
- Package: `mvn package -DskipTests` (if applicable)
- Any custom Maven profiles or flags observed

## Common Pitfalls
<Things that caused rework or test failures in past runs:>
- e.g., "Must use jakarta.* not javax.* for validation annotations"
- e.g., "Tests must mock the service layer — no @SpringBootTest for unit tests"
- e.g., "Project uses a custom base entity — all entities must extend BaseEntity"
- e.g., "GlobalExceptionHandler already exists — don't create a second one"

## Implementation History
<Brief log of what was implemented across runs:>
### Run <N>
- **ticket**: <jira ticket>
- **tasks_completed**: <count>
- **iterations_needed**: <count>
- **rework_reasons**: <brief summary of what caused rework, if any>
```

### Rules
- Be **factual** — only document patterns you actually observed in the code, not assumptions.
- Be **specific** — include actual class names, package paths, and annotation styles.
- Keep entries **concise** — this file is read by agents on every future run, so brevity matters.
- The `## Common Pitfalls` section is the most valuable — prioritize capturing rework causes.
